# تعليمات إلزامية — تطبيق «الخَطّ» (علم الرمل)

هذا التطبيق يحسب نتائج علم الرمل وتُعرض للمستخدم كجواب على أسئلة حياتية
حساسة. أي خطأ حسابي هنا ليس مجرد عطل برمجي، بل نتيجة خاطئة تُقدَّم
للمستخدم على أنها صحيحة. لذلك القواعد التالية **إلزامية** في كل جلسة عمل:

## ١. لا تصريح بلا اختبار فعلي

لا تفترض أن حساباً صحيح لمجرد أنه «يبدو منطقياً» — تحقّق منه فعلياً عبر
تشغيل الاختبارات قبل أي تصريح بأن الميزة تعمل:

```bash
node test/ramal.test.js && node test/chat.test.js
```

لا تكتب «تم التحقق» أو «يعمل بنجاح» إلا بعد رؤية نتيجة الاختبار فعلياً
في المخرجات.

## ٢. قاعدة الصحة الذهبية

مجموع نقاط الميزان (القاضي) يجب أن يكون **زوجياً دائماً**. أي تعديل على
محرك الحساب (`js/ramal.js`) يجب أن يُختبر على آلاف الضربات العشوائية قبل
اعتباره سليماً — الاختبارات الموجودة تفعل ذلك (٥٠٠٠ ضربة)، فلا تُضعِفها
ولا تحذفها، وزدها عند الحاجة.

## ٣. لا اجتهاد في البيانات العلمية

لا تُضِف أو تُغيّر بيانات علمية (أشكال، بيوت، أحكام، دلالات) من عندك أو
بناءً على معرفة عامة خارجية — كل شيء يجب أن يُطابق ما هو موجود فعلياً في
`js/data.js` أو ما وافق عليه صاحب المشروع صريحاً. عند الشك في قاعدة
تقليدية، **اسأل بدل الافتراض**.

## ٤. الصدق في درجة الثقة

عند عرض أي جواب أو نتيجة فيها غموض، صرّح بمستوى الثقة (متفق قوي / راجح /
مضطرب) بدل تقديم كل شيء بنفس درجة الجزم — هذا مبدأ «الصدق» المعتمد في
`js/chat.js`، ويسري عليك أنت أيضاً كمساعد: لا تُخرِج جواباً وكأنه مؤكد
وهو غير مختبر.

## ٥. لا دفع بلا اختبارات كاملة

قبل أي `commit`/`push` على الفرع، شغّل كامل الاختبارات وأرفق نتيجتها
الفعلية في ردّك، لا ملخصاً افتراضياً.

## ٦. درجة التفكير

استخدم أعلى درجة من التفكير المتاحة عند لمس محرك الحساب (`js/ramal.js`)
أو منطق المحادثة (`js/chat.js`) — التعديلات التصميمية البسيطة وحدها هي
التي تحتمل درجة أقل.

## مراجع سريعة

- فرع التطوير: `claude/geomancy-line-app-nh4n2r` — كل العمل يُدفع عليه
- ملخص المشروع الكامل: `PROJECT_SUMMARY.md`
- التشغيل محلياً: فتح `index.html` أو `python3 -m http.server`
